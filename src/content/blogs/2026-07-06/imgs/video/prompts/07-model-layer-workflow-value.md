16:9 video illustration in Field Deployment Editorial style.

Scene: a layered business system diagram rendered as physical architecture. Bottom layer: abstract model/API engine. Middle layer: workflow, data, permissions, evals, and tools. Top layer: business outcomes represented by speed, margin, risk reduction, and product advantage icons. The value glow should be strongest around the workflow layer, not the model layer.

Purpose: show why value is captured around the API call.

No readable labels, no charts with text, no logos, no robots, no neon dashboard, no watermark.
