# Archive

> 不再活跃但保留 git history 的内容。

## What's here

| Path | Why archived | Date |
|------|--------------|------|
| `resource/` | 仅一个 link bookmark，不再是结构化资源库 | 2026-04-13 |

> 注: `life/`, `logs/`, `skills/`, `todo/` 没有进 archive — 因为它们其实**仍在活跃使用**，只是被重命名/移动到了 brain/ 或 inbox/。见 [../../CLAUDE.md](../../CLAUDE.md) 的迁移说明。

## 何时再 archive 东西

当某个目录 / 文件：
- 6 个月没动过
- 不被任何 active workflow 引用
- 但保留它的 git history 仍有价值

→ `git mv` 进来，更新本表。
